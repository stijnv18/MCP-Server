services:
  mcp-server-dev-requests:
    build: .
    ports:
      - "3001:3001"
    environment:
      - DB_USER=copilot
      - DB_PASSWORD=--password--
      - DB_SERVER=10.232.11.14\ua
      - DB_NAME=MI_DataWarehouse
      - API_KEY=test-api-key
      - SENTRY_DSN=
      - NODE_ENV=development
      - PORT=3001
